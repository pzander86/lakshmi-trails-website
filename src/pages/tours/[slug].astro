---
import { Image } from 'astro:assets';
import Layout from '../../layouts/Layout.astro';
import Footer from '../../components/Footer.astro';

export const prerender = true;

export async function getStaticPaths() {
  let tours = [];
  try {
    tours = (await import('../../data/tours.en.json')).default || [];
  } catch (error) {
    console.error('Error loading tours data:', error);
    tours = [];
  }

  return tours.map((tour) => ({
    params: { slug: tour.slug },
    props: { tour, tours }, // Pass all tours for cross-linking
  }));
}

const { tour, tours } = Astro.props;

const structuredData = tour.schema;

// Prepare meta data for your Layout component
const meta = {
  title: `${tour.name} | Lakshmi Trails`,
  description: tour.shortDescription,
  image: `https://lakshmitrails.com${tour.image}`,
  url: `https://lakshmitrails.com/tours/${tour.slug}/`
};
---

<Layout {meta}>
  <!-- JSON-LD Structured Data -->
  <script type="application/ld+json" set:html={JSON.stringify(structuredData)}></script>

  <!-- Breadcrumb Schema -->
  <script type="application/ld+json" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": "BreadcrumbList",
    "itemListElement": [
      {
        "@type": "ListItem",
        "position": 1,
        "name": "Home",
        "item": "https://lakshmitrails.com"
      },
      {
        "@type": "ListItem", 
        "position": 2,
        "name": "Tours",
        "item": "https://lakshmitrails.com/#tours"
      },
      {
        "@type": "ListItem",
        "position": 3,
        "name": tour.title,
        "item": `https://lakshmitrails.com/tours/${tour.slug}/`
      }
    ]
  })}></script>

  <!-- SECTION 1: Hero Section -->
  <section id="hero" class="tour-hero">
    <a href="#main-content" class="skip-link">Skip to content</a>
    
    <Image
      src={tour.image}
      alt={`${tour.title} - ${tour.shortDescription}`}
      width={1600}
      height={900}
      loading="eager"
      format="webp"
      class="hero-image"
    />
    <div class="hero-overlay"></div>
    
    <!-- Simple Navigation -->
    <nav class="hero-nav" aria-label="Main navigation">
      <a href="/" class="nav-home">← Back to Home</a>
      <a href="/#tours" class="nav-tours">All Journeys</a>
    </nav>

    <div class="hero-content">
      <h1 style="color: white !important;">
        <span class="highlight" style="color: #D4AF37 !important;">{tour.title}</span>
        <br><span class="subtitle" style="color: white !important;">{tour.name}</span>
      </h1>
      <p class="tagline" style="color: rgba(249, 249, 249, 0.95) !important;">
        <span class="dates-info" style="color: #D4AF37 !important;">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true" style="color: #D4AF37 !important;">
            <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect>
            <line x1="16" y1="2" x2="16" y2="6"></line>
            <line x1="8" y1="2" x2="8" y2="6"></line>
            <line x1="3" y1="10" x2="21" y2="10"></line>
          </svg>
          {tour.dates}
        </span>
        •
        <span class="price-info" style="color: white !important;">{tour.price}</span>
      </p>
      <a href="#overview" class="cta-button" style="margin-top: 2rem;">
        <span>Explore This Journey</span>
      </a>
    </div>

    <div class="scroll-indicator" aria-hidden="true" role="presentation"></div>
  </section>

  <!-- SECTION 2: Tour Overview -->
  <main id="main-content">
    <section id="overview" class="tour-overview" role="region" aria-labelledby="overview-heading">
    <div class="overview-container">
      <header class="section-header">
        <h2 id="overview-heading">Journey Overview</h2>
        <div class="decorative-accent" aria-hidden="true"></div>
      </header>
      
      <div class="overview-content">
        <div class="main-description">
          <p class="description-intro">{tour.description}</p>
          
          <div class="tour-highlights">
            <h3>Sacred Experiences Await</h3>
            <ul class="highlights-list">
              {tour.highlights.map((highlight) => (
                <li>{highlight}</li>
              ))}
            </ul>
          </div>
        </div>

        <div class="tour-details-sidebar">
          <div class="details-card">
            <h3>Journey Details</h3>
            <div class="detail-item">
              <span class="label">Duration</span>
              <span class="value">15 Days</span>
            </div>
            <div class="detail-item">
              <span class="label">Dates</span>
              <span class="value">{tour.dates}</span>
            </div>
            <div class="detail-item">
              <span class="label">Group Size</span>
              <span class="value">12-16 Travelers</span>
            </div>
            <div class="detail-item">
              <span class="label">Investment</span>
              <span class="value price-highlight">{tour.price}</span>
            </div>
            {tour.earlyBirdPrice < tour.regularPrice && (
              <div class="early-bird-note">
                <span class="early-bird-price">Early Bird: ${tour.earlyBirdPrice}</span>
                <span class="early-bird-until">Valid until {new Date(tour.earlyBirdValidUntil).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })}</span>
              </div>
            )}
          </div>

          <div class="traveler-types">
            <h4>Perfect For</h4>
            <div class="types-list">
              {tour.touristTypes.map((type) => (
                <span class="type-tag">{type}</span>
              ))}
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- SECTION 3: Reserve Your Sacred Journey -->
  <section id="reserve" class="reserve-section" role="region" aria-labelledby="reserve-heading">
    <div class="reserve-container">
      <div class="reserve-content">
        <h2 id="reserve-heading">Reserve Your Sacred Journey</h2>
        <div class="decorative-accent" aria-hidden="true"></div>
        <p class="reserve-intro">
          Every traveler has unique needs and interests. <span class="highlight-text">Let's discuss how we can tailor this transformational experience to your personal journey.</span>
        </p>
        <div class="reserve-actions">
          <a href={tour.pdf} target="_blank" class="cta-button cta-button--outline reserve-download-btn" aria-label={`Download detailed itinerary for ${tour.title}`} style="margin-right: 1.5rem;">
            <span>Download Detailed Itinerary</span>
          </a>
          <a href="/contact" class="cta-button" aria-label={`Reserve your spot for ${tour.title}`}>
            <span>Reserve Your Spot</span>
          </a>
        </div>
        <p class="reserve-note">
          <strong>Limited to 16 travelers</strong> for intimate, authentic encounters with expert guidance throughout your cultural immersion.
        </p>
      </div>
    </div>
  </section>

  <!-- SECTION 4: Other Sacred Journeys -->
  <section id="other-journeys" class="other-journeys-section" role="region" aria-labelledby="other-journeys-heading">
    <div class="other-journeys-container">
      <header class="section-header">
        <h2 id="other-journeys-heading" style="font-family: var(--font-headline) !important; font-size: clamp(2.4rem, 4.5vw, 3.2rem) !important; color: var(--color-dark-gold) !important;">Explore Other Journeys</h2>
        <div class="decorative-accent" aria-hidden="true"></div>
        <p class="section-intro">
          Each path offers its own unique transformation. Discover which sacred journey calls to your soul.
        </p>
      </header>
      
      <div class="other-journeys-grid">
        {tours.filter(t => t.slug !== tour.slug).map((otherTour) => (
          <article class="journey-card">
            <div class="journey-image">
              <Image
                src={otherTour.image}
                alt={`${otherTour.title} - ${otherTour.shortDescription}`}
                width={400}
                height={250}
                loading="lazy"
                format="webp"
              />
              <div class="journey-price-badge">{otherTour.price}</div>
            </div>
            <div class="journey-content">
              <h3>{otherTour.title}</h3>
              <p class="journey-dates">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
                  <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect>
                  <line x1="16" y1="2" x2="16" y2="6"></line>
                  <line x1="8" y1="2" x2="8" y2="6"></line>
                  <line x1="3" y1="10" x2="21" y2="10"></line>
                </svg>
                {otherTour.dates}
              </p>
              <p class="journey-description">{otherTour.shortDescription}</p>
              <div class="journey-actions">
                <a href={otherTour.pdf || '#'} download class="cta-button cta-button--outline cta-button--small" aria-label={`Download itinerary for ${otherTour.title}`}>
                  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
                    <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
                    <polyline points="7,10 12,15 17,10"></polyline>
                    <line x1="12" y1="15" x2="12" y2="3"></line>
                  </svg>
                  <span>Itinerary</span>
                </a>
                <a href={`/tours/${otherTour.slug}/`} class="cta-button cta-button--secondary cta-button--small" aria-label={`Explore ${otherTour.title} journey`}>
                  <span>Explore Journey</span>
                  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
                    <line x1="5" y1="12" x2="19" y2="12"></line>
                    <polyline points="12,5 19,12 12,19"></polyline>
                  </svg>
                </a>
                <a href="/contact" class="cta-button cta-button--small" aria-label={`Reserve your spot for ${otherTour.title}`}>
                  <span>Reserve</span>
                  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
                    <circle cx="12" cy="12" r="10"></circle>
                    <path d="M12 6v6l4 2"></path>
                  </svg>
                </a>
              </div>
            </div>
          </article>
        ))}
      </div>
    </div>
  </section>
  </main>
  <Footer />
</Layout>

<style>
  /* Hero Section */
  #hero {
    position: relative;
    min-height: 100vh;
    width: 100%;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 2rem;
    color: var(--color-white);
  }

  .hero-image {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: 0;
  }

  .hero-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(0, 0, 0, 0.5) 0%, rgba(0, 0, 0, 0.7) 100%);
    z-index: 1;
  }

  .skip-link {
    position: absolute;
    top: -1000px;
    left: 0;
    padding: 0.5rem;
    background: var(--color-gold);
    color: #000;
    z-index: 1000;
  }

  .skip-link:focus {
    top: 0;
  }

  /* Hero Navigation */
  .hero-nav {
    position: absolute;
    top: 2rem;
    left: 2rem;
    z-index: 3;
    display: flex;
    gap: 1.5rem;
    opacity: 0;
    transform: translateY(-20px);
    animation: fadeInDown 1s cubic-bezier(0.4, 0, 0.2, 1) 0.5s forwards;
  }

  .hero-nav a {
    color: rgba(249, 249, 249, 0.9);
    text-decoration: none;
    font-size: 0.9rem;
    font-family: var(--font-body);
    font-weight: 500;
    padding: 0.5rem 1rem;
    border-radius: 20px;
    background: rgba(0, 0, 0, 0.2);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    transition: all 0.3s ease;
  }

  .hero-nav a:hover,
  .hero-nav a:focus {
    color: var(--color-white);
    background: rgba(212, 175, 55, 0.2);
    border-color: rgba(212, 175, 55, 0.3);
    transform: translateY(-1px);
    outline: none;
  }

  /* Hero Content */
  .hero-content {
    position: relative;
    z-index: 2;
    max-width: 900px;
    animation: fadeInUp 1.5s cubic-bezier(0.4, 0, 0.2, 1) forwards;
    opacity: 0;
    transform: translateY(30px);
  }

  .hero-content h1 {
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    font-weight: 400;
    margin-bottom: 2.5rem;
    line-height: 1.3;
    text-shadow: 0 4px 20px rgba(0,0,0,0.4);
    font-family: var(--font-headline);
    color: var(--color-white);
    letter-spacing: -0.02em;
  }

  .hero-content h1 .highlight {
    color: var(--color-gold);
    font-style: italic;
  }

  .hero-content h1 .subtitle {
    font-size: 0.45em;
    opacity: 0.95;
    font-weight: 300;
    font-style: normal;
  }

  .hero-content .tagline {
    color: rgba(249, 249, 249, 0.95);
    font-size: clamp(1.1rem, 2.8vw, 1.25rem);
    font-weight: 400;
    max-width: 650px;
    line-height: 1.65;
    margin: 0 auto 4rem auto;
    letter-spacing: 0.4px;
    font-family: var(--font-body);
    text-shadow: 0 2px 8px rgba(0,0,0,0.3);
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .dates-info {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--color-gold);
    font-weight: 500;
  }

  .dates-info svg {
    color: var(--color-gold);
    flex-shrink: 0;
  }

  .price-info {
    color: var(--color-white);
    font-weight: 600;
  }

  /* CTA Button styles now centralized in Layout.astro */

  .scroll-indicator {
    position: absolute;
    bottom: 3rem;
    left: 50%;
    transform: translateX(-50%);
    z-index: 2;
    opacity: 0.8;
  }

  .scroll-indicator::before {
    content: '';
    position: absolute;
    top: -10px;
    left: 50%;
    width: 1px;
    height: 30px;
    background: linear-gradient(to bottom, transparent, var(--color-white), transparent);
    transform: translateX(-50%);
    animation: scrollLine 2s infinite ease-in-out;
  }

  .scroll-indicator::after {
    content: '';
    width: 20px;
    height: 20px;
    border-right: 2px solid var(--color-white);
    border-bottom: 2px solid var(--color-white);
    transform: rotate(45deg);
    animation: scrollArrow 2s infinite ease-in-out;
    display: block;
  }

  /* FORCE SECTION ORDER WITH CSS */
  .tour-hero { order: 1; }
  .tour-overview { order: 2; }
  .reserve-section { order: 3; }
  .other-journeys-section { order: 4; }

  /* Tour Overview Section */
  .tour-overview {
    background: var(--color-mist);
    padding: 6rem 2rem 5rem 2rem;
    scroll-margin-top: 80px;
  }

  .overview-container {
    max-width: 1200px;
    margin: 0 auto;
  }

  .section-header {
    text-align: center;
    margin-bottom: 3rem;
    opacity: 0;
    transform: translateY(30px);
  }

  .section-header.intersecting {
    animation: fadeInUp 1.2s cubic-bezier(0.25, 0.46, 0.45, 0.94) 0.1s forwards;
  }

  .section-header h2 {
    font-family: var(--font-headline);
    font-size: clamp(2.2rem, 4vw, 2.8rem);
    color: var(--color-dark-gold);
    margin-bottom: 0.5rem;
    line-height: 1.2;
    font-weight: 400;
    letter-spacing: -0.01em;
  }

  .decorative-accent {
    width: 60px;
    height: 2px;
    background: linear-gradient(135deg, var(--color-gold) 0%, var(--color-dark-gold) 100%);
    margin: 1.5rem auto 0 auto;
    position: relative;
    border-radius: 1px;
  }

  .decorative-accent::before,
  .decorative-accent::after {
    content: '';
    position: absolute;
    width: 6px;
    height: 6px;
    background: var(--color-gold);
    border-radius: 50%;
    top: -2px;
  }

  .decorative-accent::before {
    left: -10px;
  }

  .decorative-accent::after {
    right: -10px;
  }

  .overview-content {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: 3rem;
    opacity: 0;
    transform: translateY(30px);
  }

  .overview-content.intersecting {
    animation: fadeInUp 1.2s cubic-bezier(0.25, 0.46, 0.45, 0.94) 0.3s forwards;
  }

  .description-intro {
    font-size: clamp(1.1rem, 2.5vw, 1.25rem);
    line-height: 1.7;
    color: var(--color-stone);
    margin-bottom: 2.5rem;
    font-weight: 400;
    font-style: italic;
    font-family: var(--font-body);
  }

  .tour-highlights h3 {
    font-family: var(--font-headline);
    color: var(--color-dark-gold);
    font-size: clamp(1.4rem, 2.5vw, 1.6rem);
    margin-bottom: 1.25rem;
    font-weight: 400;
  }

  .highlights-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .highlights-list li {
    padding: 0.75rem 0;
    border-bottom: 1px solid rgba(212, 175, 55, 0.1);
    font-size: clamp(0.95rem, 1.8vw, 1.05rem);
    line-height: 1.6;
    color: var(--color-stone);
    font-family: var(--font-body);
    position: relative;
    padding-left: 1.5rem;
  }

  .highlights-list li:before {
    content: '✦';
    position: absolute;
    left: 0;
    color: var(--color-gold);
    font-size: 1em;
  }

  .highlights-list li:last-child {
    border-bottom: none;
  }

  /* Sidebar */
  .tour-details-sidebar {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .details-card {
    background: rgba(255, 255, 255, 0.95);
    padding: 2rem 1.75rem;
    border-radius: 16px;
    border: 1px solid rgba(212, 175, 55, 0.08);
    box-shadow: 
      0 10px 40px rgba(51, 51, 51, 0.08),
      0 4px 20px rgba(0, 0, 0, 0.05);
    transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    backdrop-filter: blur(10px);
    position: relative;
  }

  .details-card:hover {
    transform: translateY(-3px);
    box-shadow: 
      0 20px 60px rgba(51, 51, 51, 0.12),
      0 8px 30px rgba(0, 0, 0, 0.08);
  }

  .details-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: linear-gradient(135deg, var(--color-gold) 0%, var(--color-dark-gold) 100%);
    border-radius: 16px 16px 0 0;
  }

  .details-card h3 {
    font-family: var(--font-headline);
    color: var(--color-dark-gold);
    font-size: 1.2rem;
    margin-bottom: 1.25rem;
    font-weight: 400;
  }

  .detail-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.65rem 0;
    border-bottom: 1px solid rgba(212, 175, 55, 0.08);
  }

  .detail-item:last-child {
    border-bottom: none;
  }

  .detail-item .label {
    color: var(--color-stone);
    font-weight: 400;
    font-family: var(--font-body);
    font-size: 0.95rem;
  }

  .detail-item .value {
    color: var(--color-dark-gold);
    font-weight: 500;
    font-family: var(--font-body);
    font-size: 0.95rem;
  }

  .price-highlight {
    color: var(--color-coral) !important;
    font-size: 1.05em;
    font-weight: 600;
  }

  .early-bird-note {
    margin-top: 0.75rem;
    padding: 0.75rem;
    background: linear-gradient(135deg, rgba(212, 175, 55, 0.05) 0%, rgba(212, 175, 55, 0.03) 100%);
    border-radius: 6px;
    border: 1px solid rgba(212, 175, 55, 0.15);
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .early-bird-price {
    color: var(--color-dark-gold);
    font-weight: 600;
    font-size: 1em;
  }

  .early-bird-until {
    color: var(--color-stone);
    font-size: 0.85em;
  }

  .traveler-types h4 {
    font-family: var(--font-headline);
    color: var(--color-dark-gold);
    font-size: 1rem;
    margin-bottom: 0.75rem;
    font-weight: 400;
  }

  .types-list {
    display: flex;
    flex-wrap: wrap;
    gap: 0.25rem;
  }

  .type-tag {
    background: linear-gradient(135deg, var(--color-gold) 0%, var(--color-dark-gold) 100%);
    color: #000;
    padding: 0.25rem 0.5rem;
    border-radius: 10px;
    font-size: 0.75rem;
    font-weight: 500;
    font-family: var(--font-body);
  }

  /* Reserve Section */
  .reserve-section {
    background: var(--color-mist);
    padding: 5rem 2rem;
    text-align: center;
    scroll-margin-top: 80px;
  }

  .reserve-container {
    max-width: 700px;
    margin: 0 auto;
  }

  .reserve-content {
    opacity: 0;
    transform: translateY(30px);
  }

  .reserve-content.intersecting {
    animation: fadeInUp 1.2s cubic-bezier(0.25, 0.46, 0.45, 0.94) 0.2s forwards;
  }

  .reserve-content h2 {
    font-family: var(--font-headline);
    font-size: clamp(2.2rem, 4vw, 2.8rem);
    color: var(--color-dark-gold);
    margin-bottom: 0.5rem;
    font-weight: 400;
    line-height: 1.2;
    letter-spacing: -0.01em;
  }

  .reserve-intro {
    font-size: clamp(1rem, 2.2vw, 1.15rem);
    line-height: 1.65;
    color: var(--color-stone);
    margin: 2.5rem auto;
    font-weight: 400;
    font-family: var(--font-body);
    max-width: 600px;
  }

  .highlight-text {
    color: var(--color-dark-gold);
    font-style: italic;
    font-weight: 500;
  }

  .reserve-actions {
    display: flex;
    justify-content: center;
    gap: 1.25rem;
    margin: 2.5rem 0;
    flex-wrap: wrap;
  }

  .reserve-note {
    font-size: clamp(0.95rem, 1.8vw, 1.05rem);
    color: var(--color-stone);
    font-family: var(--font-body);
    margin-top: 1.5rem;
  }

  .reserve-note strong {
    color: var(--color-dark-gold);
  }

  /* Fix reserve download button text visibility */
  .reserve-download-btn:hover,
  .reserve-download-btn:focus {
    color: #000 !important;
  }
  
  .reserve-download-btn:hover *,
  .reserve-download-btn:focus * {
    color: #000 !important;
  }

  /* Other Sacred Journeys Section */
  .other-journeys-section {
    background: var(--color-mist);
    padding: 1rem 2rem 6rem 2rem;
    scroll-margin-top: 80px;
  }

  .other-journeys-container {
    max-width: 1200px;
    margin: 0 auto;
  }

  .section-intro {
    font-size: clamp(1rem, 2.2vw, 1.15rem);
    line-height: 1.65;
    color: var(--color-stone);
    margin: 2rem auto 0 auto;
    font-weight: 400;
    font-family: var(--font-body);
    max-width: 550px;
  }

  .other-journeys-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(360px, 1fr));
    gap: 1.75rem;
    margin-top: 3.5rem;
    opacity: 0;
    transform: translateY(30px);
  }

  .other-journeys-grid.intersecting {
    animation: fadeInUp 1.2s cubic-bezier(0.25, 0.46, 0.45, 0.94) 0.3s forwards;
  }

  .journey-card {
    background: var(--color-white);
    border-radius: 12px;
    box-shadow: 0 6px 25px rgba(51, 51, 51, 0.06);
    border: 1px solid rgba(212, 175, 55, 0.08);
    overflow: hidden;
    display: flex;
    flex-direction: column;
    height: 100%;
    transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }

  .journey-card:hover {
    transform: translateY(-6px);
    box-shadow: 0 15px 40px rgba(51, 51, 51, 0.1);
    border-color: rgba(212, 175, 55, 0.15);
  }

  .journey-image {
    height: 220px;
    overflow: hidden;
    position: relative;
  }

  .journey-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.3s ease;
  }

  .journey-card:hover .journey-image img {
    transform: scale(1.03);
  }

  .journey-price-badge {
    position: absolute;
    top: 0.75rem;
    right: 0.75rem;
    background: linear-gradient(135deg, var(--color-gold) 0%, var(--color-dark-gold) 100%);
    color: #000;
    padding: 0.4rem 0.8rem;
    border-radius: 16px;
    font-size: 0.85rem;
    font-weight: 500;
    box-shadow: 0 3px 12px rgba(212, 175, 55, 0.25);
    font-family: var(--font-body);
  }

  .journey-content {
    padding: 1.75rem;
    flex-grow: 1;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .journey-content h3 {
    font-family: var(--font-headline);
    color: var(--color-dark-gold);
    font-size: 1.35rem;
    margin: 0;
    font-weight: 400;
    line-height: 1.3;
  }

  .journey-dates {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    color: var(--color-stone);
    font-size: 0.9rem;
    font-weight: 400;
    font-family: var(--font-body);
  }

  .journey-dates svg {
    color: var(--color-gold);
    flex-shrink: 0;
  }

  .journey-description {
    color: var(--color-stone);
  }

  .journey-actions {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 0.5rem;
    margin-top: auto;
    padding-top: 1rem;
  }

  /* Fix journey card button text visibility */
  .journey-actions .cta-button:hover,
  .journey-actions .cta-button:focus {
    color: #000 !important;
  }
  
  .journey-actions .cta-button:hover *,
  .journey-actions .cta-button:focus * {
    color: #000 !important;
  }

  .journey-description {
    font-size: 0.95rem;
    line-height: 1.55;
    font-weight: 400;
    flex-grow: 1;
    margin: 0;
    font-family: var(--font-body);
  }

  /* Animations */
  @keyframes fadeInUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes fadeInDown {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes scrollLine {
    0%, 100% { opacity: 0; }
    50% { opacity: 1; }
  }

  @keyframes scrollArrow {
    0%, 20%, 50%, 80%, 100% {
      transform: rotate(45deg) translateY(0);
      opacity: 0.6;
    }
    40% {
      transform: rotate(45deg) translateY(-8px);
      opacity: 1;
    }
    60% {
      transform: rotate(45deg) translateY(-4px);
      opacity: 0.8;
    }
  }

  /* Responsive Design */
  @media (max-width: 968px) {
    .hero-nav {
      top: 1rem;
      left: 1rem;
      flex-direction: column;
      gap: 0.75rem;
    }

    .hero-nav a {
      font-size: 0.85rem;
      padding: 0.4rem 0.8rem;
    }

    .hero-content .tagline {
      flex-direction: column;
      gap: 0.5rem;
    }

    .overview-content {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .other-journeys-grid {
      grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    }

    .reserve-actions {
      flex-direction: column;
      align-items: center;
    }

    .tour-overview {
      padding: 5rem 2rem 4rem 2rem;
    }

    .reserve-section {
      padding: 4rem 2rem 1rem 2rem;
    }

    .other-journeys-section {
      padding: 1rem 2rem 5rem 2rem;
    }

    .decorative-accent {
      width: 60px;
      margin: 1.5rem auto 0 auto;
    }
  }

  @media (max-width: 768px) {
    .other-journeys-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .journey-image {
      height: 200px;
    }

    .journey-content {
      padding: 1.5rem;
    }

    .journey-content h3 {
      font-size: 1.4rem;
    }

    .journey-description {
      font-size: 0.95rem;
    }

    .journey-actions {
      grid-template-columns: 1fr;
      gap: 0.75rem;
    }
  }

  @media (max-width: 480px) {
    #hero {
      min-height: 90vh;
      padding: 1rem;
    }

    .hero-nav {
      top: 0.5rem;
      left: 0.5rem;
    }

    .hero-nav a {
      font-size: 0.8rem;
      padding: 0.3rem 0.6rem;
    }

    .tour-overview,
    .reserve-section,
    .other-journeys-section {
      padding: 4rem 1.5rem 1rem 1.5rem;
    }

    .other-journeys-section {
      padding: 1rem 1.5rem 4rem 1.5rem;
    }

    .details-card {
      padding: 2rem 1.5rem;
    }

    .journey-content {
      padding: 1.25rem;
    }
    
    .journey-price-badge {
      top: 0.75rem;
      right: 0.75rem;
      padding: 0.4rem 0.8rem;
      font-size: 0.85rem;
    }
  }

  /* Reduced motion preferences */
  @media (prefers-reduced-motion: reduce) {
    .hero-content,
    .hero-nav,
    .section-header,
    .overview-content,
    .other-journeys-grid,
    .reserve-content {
      animation: none;
      opacity: 1;
      transform: none;
    }
    
    .scroll-indicator::before,
    .scroll-indicator::after {
      animation: none;
    }
    
    .details-card,
    .journey-card,
    .cta-button,
    .hero-nav a {
      transition: none;
    }
    
    .details-card:hover,
    .journey-card:hover,
    .cta-button:hover,
    .cta-button:focus,
    .hero-nav a:hover,
    .hero-nav a:focus {
      transform: none;
    }

    .journey-image img {
      transition: none;
    }

    .journey-card:hover .journey-image img {
      transform: none;
    }
  }

  /* High contrast mode support */
  @media (prefers-contrast: high) {
    .details-card,
    .journey-card {
      background: rgba(249, 249, 249, 0.98);
      border-color: var(--color-stone);
    }
    
    .section-header h2,
    .reserve-content h2,
    .highlight-text,
    .hero-content h1 .highlight {
      text-shadow: none;
    }

    .hero-nav a {
      background: rgba(0, 0, 0, 0.4);
      border-color: rgba(255, 255, 255, 0.3);
    }
  }
</style>

